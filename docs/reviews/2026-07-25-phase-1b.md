# Ревью Фазы 1b (2026-07-25) — реестр находок

Диф: `c17338a..39dd8ec` (52 файла, +7469). Ревьюеры: maintainability (M), kieran-python (K),
security (SEC), correctness (C), adversarial (A), testing (T) — по зонам, независимо, чистый контекст.

**Вердикт на входе в докрутку: выдавать ученикам НЕЛЬЗЯ.** Два критических пути к root и четыре
блокера, из-за которых установка на живом VPS не доходит до конца. Ни один скрипт до ревью на
VPS не запускался — все находки-блокеры лежат ровно в этой непроверенной зоне.

## Клубок №1 — права и владение файлами (корень половины блокеров)

Три документа требуют взаимоисключающего, поэтому «работающей» конфигурации не существует:
`install.sh:355` отдаёт весь движок run-user'у (нужно для `uv sync` в `.venv`), а
`deploy/install-sudoers.sh:77-84` и `docs/deploy/rights-model.md:45-51` требуют root-владения
(иначе агент перепишет скрипт, на который выдан NOPASSWD).

| ID | Что | Файл |
|---|---|---|
| SEC-1 🔴крит | `update.sh --engine-dir <свой путь>` → `source $ENGINE_DIR/deploy/_common.sh` от root **до** проверки dry-run. Агент пишет свой `_common.sh` → root. PoC подтверждён. В sudoers команда без аргументов = «с любыми аргументами» | update.sh:33,57-60; deploy/sudoers.d/unpacker:21 |
| SEC-2 🔴крит | `chown -R run-user /opt/unpacker`: все whitelisted-скрипты и всё, что они сорсят, перезаписываемы агентом. Спусковой крючок — документированный `sudo update.sh`. Второй путь: подмена `.install.conf`, который root сорсит | install.sh:355,402 |
| C5/SEC-6 | Следствие: `install-sudoers.sh` честно откажет → whitelist не встанет → продукт из коробки не работает, а «починка руками» ведёт в SEC-2 | install.sh:355 ↔ install-sudoers.sh:77 |
| SEC-3 | NOPASSWD выдаётся на ещё не существующий `update.sh`; ветка «owner == run-user» только предупреждает; `deploy.sh` зовёт `install-sudoers.sh` от root, обходя правило «его нет в whitelist» | install-sudoers.sh:63-92 |
| SEC-8 | `UV_BIN` резолвится через login-шелл run-user'а и подставляется sed'ом в root-устанавливаемый юнит. Сегодня не эксплуатируется (sed падает), но граница доверия неверная | _common.sh:38-45; deploy.sh:510 |

**Решение клубка (одно, сверху):** код `/opt/unpacker` — `root:root`, `go-w`; venv наружу через
`UV_PROJECT_ENVIRONMENT`; `git`-операции и `uv sync` — от root; run-user получает write только на
инстансы и venv; `.install.conf` парсить, а не `source`, и держать вне каталога движка;
секреты — через файл/stdin, не argv; в sudoers перечислить разрешённые формы вызова.

## Блокеры установки (install.sh/update.sh никогда не бежали живьём)

| ID | Что | Файл |
|---|---|---|
| C1 | При запуске от root (документированный путь!) `$SUDO -u ...` → `-u: command not found`, `set -e` убивает установку. Ломает и печатаемую инструкцию `claude setup-token` | install.sh:72-76,300,461,521 |
| C2/M-03 | `install.sh` встаёт на тег (detached HEAD) → `deploy.sh:361 git pull --ff-only` → exit 3 «не раскатываю на битом коде». Ломает и каждый последующий деплой, который делает Распаковщик | install.sh:347 ↔ deploy.sh:360 |
| C3/M-01/SEC-6 | `install.sh` не передаёт `--role unpacker` → нет drop-in (`NoNewPrivileges=no`) и нет sudoers → мета-агент не может развернуть ни одного бота. Главное обещание README мертво | install.sh:492 ↔ deploy.sh:472 |
| C4 | `uv` ставится в `/root/.local/bin` (700), а нужен в контексте run-user → preflight ПРОВАЛ на деплое, после того как ученик уже ввёл все ответы | install.sh:203 ↔ _common.sh:41 |
| C15 | Нет `apt-get update`, `apt-get install` без обработки отказа → на VPS с устаревшими индексами установка падает сырой ошибкой apt | install.sh:269,146 |
| C22 | README велит клонировать приватный репо ДО установщика, а помощь со входом в GitHub живёт внутри установщика — ученик стоит на первой команде | README ↔ install.sh:343 |
| C16 | После `chown` git-вызовы от root упираются в `safe.directory` → `update.sh` печатает «в репо нет тегов» на репо с тегами | install.sh:355 ↔ update.sh:70-105 |

## Эксплуатация: тихие обманы («печатает готово, а не сделал»)

| ID | Что | Файл |
|---|---|---|
| C6 | `update.sh` от root (как сам и советует): RUN_USER=root → бэкапов баз нет, боты не рестартованы, `uv sync` под root — и финальное «готово: v1 → v2» | update.sh:57-65,121,179,209 |
| C7 | Повторный деплой молча игнорирует изменённый `--users`: ученик исправил опечатку в своём Telegram id, `.env` не тронут, бот по-прежнему молчит, install.sh рапортует «Бот поднят» | deploy.sh:395 |
| C8 | Ротация cc-token не рестартует юнит → протухший токен продолжает жить в процессе; инструкция замкнута в цикл | deploy.sh:397-406 |
| C12 | `update.sh` рестартует юнит для каждой папки без проверки существования юнита → `set -e` рвёт обновление на середине, парк разнородный | update.sh:190 |
| C13 | Провал `uv sync` после checkout: код новый, зависимости старые (в дифе добавлен pyyaml), про `--rollback` не сказано | update.sh:162-171 |
| C21 | `.env` без строки токена считается «повторным деплоем того же бота» → токен не сохраняется, движок падает на обязательном поле, скрипт печатает «готово» | deploy.sh:265-275 |
| C9 | Передеплой умеет смешать контуры auth (api + subscription в одном .env) — состояние, которое гейт §8.1 объявляет запрещённым | deploy.sh:220 ↔ 395 |
| C14/M-20 | `agentctl doctor` печатается без sudo/TG_RUN_USER → на живого бота отвечает `NOT DEPLOYED`, и скилл `operate` ведёт по ложному следу | install.sh:529, README |
| M-18 | README/install.sh печатают `sudo bash update.sh`, а whitelist разрешает вызов без `bash` → «user is not allowed to execute /bin/bash» | README:243 |

## Песочница и секреты (подтверждено 4 ревьюерами независимо)

| ID | Что | Файл |
|---|---|---|
| K1/M-07/SEC-5/C17 | Корень песочницы `[SEND_FILE:]` = `parent(db_path)`. Дефолт `db_path="state.db"` → корнем становится каталог инстанса, где лежит `.env` с токеном бота И токеном подписки. Deny-list по именам отсутствует | runtime.py:109 ↔ config.py:36 |
| SEC-4 | Токены уходят в argv `sudo` → навсегда в `/var/log/auth.log`; cc-token дополнительно в history root и в чат Telegram (и в `~/.claude/projects/*.jsonl`, читаемые агентом) | SKILL.md, install.sh:493,525 |
| SEC-9 | Песочница — guardrail против ошибки модели, а не sandbox против агента с Bash (`cp secret brain/notes.txt` + TOCTOU). Docstring продаёт её как защиту секретов | sendfile.py:1-20 |
| SEC-7 | Гейт аудитории только deploy-time: `AUDIENCE` в `.env` не пишется, рантайм про контур не знает; допиской id в ALLOWED_USER_IDS internal превращается в клиентский без гейта | deploy.sh:210 ↔ 415 |

## Мозг Распаковщика и документы (исполняемая часть продукта)

| ID | Что | Файл |
|---|---|---|
| M-04 | Контракт `[SEND_FILE:путь]` не доходит до модели: `system_prompt_append` пуст, в шаблонах мозга маркер не упомянут — а `_template`/`examples` обещают «пришлю файлом». Фича мертва | sendfile.py ↔ runtime.py:52 |
| M-05/C20 | Отчёты велено писать в относительный `deploys/` → пишутся внутрь папки-мозга (что README сам запрещает) и делают git-мозг грязным → гейт чистоты блокирует следующий деплой | brains/unpacker/CLAUDE.md:74 |
| M-06/C10 | Скилл обещает `/usage`, которой в боте нет (README относит её к Фазе 2), а тест закрепляет обещание. Владелец получит выдуманные цифры | operate/SKILL.md:75; test_unpacker_brain.py:173 |
| M-08/C11 | Мозг копируется один раз и больше не обновляется: `update.sh` мозги не трогает, передеплой видит непустую папку и не трогает. Правки скиллов не доезжают до учеников никогда | deploy.sh:375 |
| C19 | Скилл велит рестартовать юнит после правки `buttons.yaml`, хотя реестр перечитывает файл сам; для самого Распаковщика это самоубийство посреди диалога | operate/SKILL.md ↔ buttons.py:33 |
| M-15 | Примеры учат относительному пути мозга (`examples/assistant`) — гейт отвергнет, cwd бота = папка-мозг | examples/README.md:9 |
| M-09 | Мёртвые ссылки «этого ещё нет» про install.sh/update.sh, которые уже в репо | rights-model.md:75; install-sudoers.sh:67 |
| M-10 | README противоречит себе: кнопки и файлы в разделе «чего ещё нет», хотя это и есть содержимое дифа | README.md:32 |
| M-13/C23 | `.env.template` объявляет себя картой настроек, но не знает про 6 новых ручек 1b; при этом пишет `AUTH_MODE`/`LIMIT_*`, которых ядро не читает | templates/.env.template:10 |
| M-16 | Список команд в README и `_HELP` бота разошлись (`/switch`, `/verbose` отсутствуют; `/projects` описан неверно) | README.md:181 |
| M-11 | Формула потолка пула в bash и python разошлась по семантике (`max(1,…)` vs блокер), README утверждает третье | install.sh:116 ↔ pool.py:62 |
| M-12 | Четвёртый вопрос установщика — тупик: выбор «API-ключ» гарантированно обрывает установку, при том что `deploy.sh` контур api поддерживает | install.sh:431 |
| M-14 | Поля паспорта мозга (name/slug/default_model) описаны ученику как работающие, потребителя нет — читается только `buttons` | brains/_template/.brain.yaml |
| M-17 | Имя юнита `agent-tg@` зашито в шести файлах, `_common.sh` его не отдаёт | _common.sh |

## Python-качество (K)

K2 stringly-typed очередь отправки с `type: ignore` · K3 `_run_system_action(action: str)` теряет
готовый `Literal` и глотает неизвестное действие · K4 `Any` + getattr-цепочки на aiogram-типах
внутри aiogram-адаптера · K5 относительный путь резолвится от `_roots[0]` (неявный контракт на
порядок) · K6 `Reason = str` вместо Literal · K7 инстансный `buttons.yaml` парсится снисходительно,
хотя правит его человек: опечатка `button:` = тихий ноль кнопок · K8 `callback.message` через
getattr → `InaccessibleMessage` проходит гейт, `thread_id` теряется · K9 mypy не строгий, новый код
этим уже пользуется · K10 три разных механизма для трёх однотипных флагов косметики ·
K11/C18 голос/видео/аудио без хендлера — бот молчит, хотя `/help` обещает приём файлов ·
K12 `IntakeResult` инвариант «никогда оба» ничем не удержан · K13 песочница молча глотает
нерезолвящийся корень · K14 «Файл 20 МБ — можно до 20 МБ» (округление) · K15-K21 мелкие
(локальный import re, дубль slug-паттерна в двух модулях, `list(enumerate())` в цикле, assert как
управляющий поток, сырой маркер в чат при выключенной отдаче).

## Адверсарный проход: что сломается у первого же ученика (ADV)

Сценарий: новичок в Linux, свежий Ubuntu, README открыт, автора рядом нет.

| ID | Что | Файл |
|---|---|---|
| ADV-01 🔴 | `uv` ставится от root (`/root/.local/bin`, 700) **до** создания юзера движка → на шаге 5 preflight ПРОВАЛ. Ломается у 100% учеников с чистым VPS, причём hardening/юзер/токен уже применены | install.sh:203-208 |
| ADV-02 🔴 | Курица-яйцо: README велит `git clone` приватного репо, а дружелюбный вход по коду живёт внутри установщика, который ещё не скачан. Плюс подсказка `apt-get install -y gh` невыполнима — в стандартных репозиториях Ubuntu пакета `gh` нет | README:96 ↔ install.sh:343 |
| ADV-08 | README учит держать мозги в `~/brains` под root (`/root`, mode 700). Гейты читают root'ом → зелёные, материализация мозга идёт `asuser tar` → `Permission denied` посреди провизии. Тот же корень: ответ «/root/brains» на вопрос 3 обрывает установку сразу после ввода токена | README:151 ↔ deploy.sh:379 |
| ADV-09 | `TG_BRAINS_BASE` живёт только в процессе установщика и стирается `env_reset` при `sudo` → первый мозг лёг в `/srv/brains`, все последующие Распаковщик кладёт в `/home/unpacker/brains`. Ученик правит один файл, бот читает другой | install.sh:460 ↔ _common.sh:29 |
| ADV-12 | Бэкап баз: `sqlite3` в install.sh не ставится, поэтому штатным путём становится `cp` живой WAL-базы без `-wal` → копия битая или без последних сессий. При этом `--rollback` откатывает только код, восстановления БД нет вообще | update.sh:129-144 |
| ADV-13 | Двойное нажатие кнопки = два полных прогона агента (квота подписки), очередь не дедуплицируется, `/stop` её не чистит. Хуже: callback несёт только индекс — после правки `buttons.yaml` старая кнопка в истории выполняет ЧУЖОЙ промпт | bot.py:284-322; keyboard.py:57 |
| ADV-14 | `reserve()` не атомарен (проверка `exists()`, создание позже в `download`) → два документа с одним именем перетирают друг друга. Ровно та тихая потеря данных, которую docstring обещает предотвратить | uploads.py:92-115 |
| ADV-15 | `ufw allow OpenSSH` открывает ровно 22/tcp. Если sshd на другом порту — текущая сессия выживает (ESTABLISHED), а следующий вход невозможен: **потеря сервера**. Плюс установщик ставит tmux, но не предлагает им воспользоваться, хотя обрыв ssh убивает установку в произвольной точке | install.sh:220-231 |
| ADV-16 | `claude`, поставленный учеником заранее от root, проходит проверку `have claude` (оболочка root) → под run-user CLI нет → бот поднят, но молчит на каждое сообщение, а `doctor` говорит HEALTHY (наличие claude он не проверяет) | install.sh:296 ↔ deploy.sh:330 |

**Общий корень ADV-05/06/09:** `resolve_run_identity` выводит личность из окружения вызывающего
(`TG_RUN_USER` → `SUDO_USER` → `id -un`). Любая команда, запущенная «не так, как это делал
install.sh», читает другую вселенную путей — и тихо рапортует успех. Лечится машинным конфигом
`/etc/unpacker/engine.conf` (run-user, agents/brains base, runtime), который читает `_common.sh`.

Ещё из residual: ни `install.sh`, ни `deploy.sh` не имеют trap-очистки — обрыв оставляет инстанс с
боевым `.env` без юнита и `TMP_ENV` с токеном в `/tmp`; псевдо-стриминг показывает сырой маркер
`[SEND_FILE:/home/...]` в черновике (вырезается только в финале); `uploads/` не чистится никогда
при пороге диска 3 ГБ.

## Честность тестов (ревью тестов, T)

287 новых тестов: ядро (песочница, uploads, кнопки, паспорт, гейты deploy.sh) покрыто
по-настоящему — subprocess-чёрный ящик, дублёры вместо моков, негативный тест на каждую атаку.
Но три шва держатся на ощущении покрытия:

| ID | Что | Файл |
|---|---|---|
| H1 | Тест **закрепляет дефект как контракт**: сверяет argv установщика и не требует `--role unpacker`. 324 строки `test_rights_model.py` проверяют артефакты, которые продукт на реальной установке не применяет | test_install_scripts.py:480-509 |
| H2 | Проводка aiogram не проверяется ничем: 40+ тестов дергают приватные методы напрямую. Убери регистрацию callback или поставь `F.text` выше `F.document` — тесты зелёные, у ученика кнопки и файлы молча мертвы | bot.py:126-142 |
| H3 | Единственный живой прогон `--role unpacker` зелёный при **полностью не установленных правах**: `install-sudoers.sh` выходит с 3 (runtime принадлежит тест-юзеру, mode 755), `deploy.sh` глотает это в warn | test_unpacker_brain.py:234-269 |
| H4 | Lint sudoers не проверяет **поле пользователя**: правило `ALL ALL=(root) NOPASSWD: …` (права всем юзерам машины) проходит все тесты. А `assert ME in body` выполняется случайно — имя юзера входит в путь runtime | test_rights_model.py:61-70 |
| H5 | Гейт «visudo забраковал рендер» — самый дорогой fail-closed в проекте (битый `/etc/sudoers.d` ломает sudo целиком) — без негативного теста | install-sudoers.sh:106-111 |
| H6 | Деплой-тесты написаны под macOS: на объявленном CI (ubuntu-latest, passwordless sudo) они уходят в systemd-ветку и **мутируют хост** — `install` в `/etc/systemd/system`, `enable --now`. Пути захардкожены, переопределить нечем | test_deploy_scripts.py:248-290 |
| H7 | `assert "6" in out` — пустой ассерт: цифра приходит из «chmod 600» в том же выводе. Потолок пула можно сломать любым способом, тест держится | test_install_scripts.py:140-157 |
| M1 | Поставляемые паспорта проверяются двумя самописными YAML-парсерами вместо продакшн-схемы, а обоснование («pyyaml нет в зависимостях») устарело в этом же дифе | test_unpacker_brain.py:54-89 |
| M3/M4 | Фразовые грепы по скиллам («одним блоком», «409», «не выдумыва») — обещания поведения как подстроки; эвристика «скрипт позвали» пропускает `agentctl.sh restart` | test_unpacker_brain.py:134-175 |
| M5 | `test_update_backs_up_state_db_before_switching` не проверяет «before» — начни бэкапить после миграций, тест зелёный | test_install_scripts.py:748 |
| M8 | Дублёр Bot никогда не падает → ветки «ответ не потерян» (сбой `send_document`, фолбэк MarkdownV2→plain) не покрыты. «Стоп» в свежем окне врёт «⏹ Прервал» | test_bot_attachments.py:47-64 |
| M9/S1 | Session-фикстура `isolated_runtime` растащена импортами между тест-модулями (нет `conftest.py`) и один тест её мутирует; три копии `run_deploy`, четыре способа сделать тест-мозг, мёртвый `if False else` | test_deploy_gates.py:26 |

Оправданные грепы (не трогать, это контракт-тесты): сверка флагов из документов мозга с
фактическим содержимым скриптов; разбор правил sudoers как артефакта; `git check-ignore`;
проверка отсутствия живых токенов в выдаваемом репо; факт вызова через стаб с логом argv.

## Пробелы тестов (T-зона, сводно)

- Ни один тест не исполняет `install.sh` от root (`id -u = 0`) — там живут C1 и C4.
- Стык `install.sh → deploy.sh` только через заглушку без гейтов → C2, C3, C7 невидимы. Нужен
  интеграционный: локальный bare-репо с тегом → install.sh → настоящий `deploy.sh --dry-run`.
- Нет негативного теста на `update.sh --engine-dir <подложенный _common.sh>` (SEC-1, PoC работает).
- Нет пост-условия install.sh: «ни один whitelisted-скрипт не writable для run-user».
- `test_installer_warns_when_update_sh_absent` закрепляет как желаемое выдачу NOPASSWD на
  несуществующий путь — тест надо перевернуть.
- Песочница не покрыта на дефолтном `DB_PATH` (там и живёт утечка `.env`), на hardlink и на TOCTOU.
- Нет сверки документов с кодом: команды из `brains/**/*.md` и README против зарегистрированных в
  `bot.py` (поймало бы `/usage`), поля `Settings` против `.env.template`, поля паспорта против
  потребителей, флаги скриптов в скиллах против `--help` скриптов (сейчас карта покрывает 3 из 5).
